import type { CalendarEvent } from './types.js'

/**
 * Parse an ICS string and return calendar events.
 * Currently a stub — returns an empty array.
 */
export function parseICS(_icsContent: string): CalendarEvent[] {
  return []
}
