# USB Protocol Analysis Commands\n# ================================\n\n# Monitor USB traffic (requires root):\n# sudo usbmon -i 3  # Bus 3 where audio controller is\n# sudo tcpdump -i usbmon3 -w hidock_traffic.pcap\n\n# HID report monitoring:\n\n# Audio stream analysis:\n# Record raw audio: arecord -D hw:2,0 -f S16_LE -r 48000 -c 2 test.wav\n# Play test tone: speaker-test -c 2 -D hw:2,0 -t sine\n
