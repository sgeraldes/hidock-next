# Nightly Roadmap — 2026-07-10

Autonomous overnight backlog. Worked one/two agents at a time (low system
footprint — transcription queue paused, no needless app restarts). Each item:
build in an isolated worktree → gate (typecheck/lint/targeted tests) → cherry-pick
onto the branch → dogfood when it's a visible change → mark done → next.

Legend: **P0** blocking · **P1** quality/noise · **P2** polish/nice-to-have.

---

## Track H — URGENT owner feedback (2026-07-10 late) — DO FIRST
- [x] **H1 (P0)** STOP transcriptions — DONE (cancelled; queue empty; frees the main process).
- [x] **H2 (P0)** Waveform must **MATCH THE MOCKUP**: colored bars + a sentiment curve on a subtle gradient panel + **numbered circular markers sitting ON the curve** + the event list below. — DONE (`e5c6807d`): WaveformPlayer full mode rebuilt to the approved composition (gradient stage panel, ＋/－ axis labels, markers-on-curve, bright playhead + play-zone, per-speaker bars, time axis, cross-linked event list). Verified live via HMR screenshot.
- [x] **H3 (P0)** Action items are **DUPLICATED** — shown in the timeline event-list AND again in the transcript/summary. — DONE (`e5c6807d`): action items now have ONE home (the timeline event-list); TranscriptViewer rendered with `showActionItems={false}`.
- [x] **H4 (P1)** **REMOVE the on-hover checkbox** from library rows entirely (no reveal-on-hover). — DONE (`f1db2204`): `SourceRow` renders no checkbox in any state; props kept for caller compat; tests rewritten (15/15).
- [x] **H5 (P0)** Kill the ugly **"Loading waveform…" overlay**. Generate peaks ONCE, **CACHE ON DISK**, load instantly. — DONE (`e5c6807d`): new `waveform-cache.ts` (+IPC+preload) stores one JSON per recording at `<userData>/cache/waveform/<id>.json`; `useAudioPlayback` loads from cache first (no loading state), computes+persists on miss; half-drawn overlay replaced by a clean "Preparing waveform…" placeholder. ⚠️ cache/IPC is main-process — needs app restart to activate.
- [x] **H6 (P0)** **"Transcript not available" + no-color waveform** when selecting via sidebar Library nav. — DONE (`e5c6807d`): SourceReader fetches the transcript directly (`transcripts.getByRecordingId`) as a fallback → feeds speaker-range colors AND the viewer, so both render on first paint regardless of selection path.
- [x] **H7 (P0)** Meeting/calendar row icons randomly disappear; lists take forever / never load; **Refresh spins with no activity**. — DONE (`eeed308d`): the P0 root cause was a synchronous ~1878-meeting calendar-sync upsert blocking the main event loop; now chunked (200/txn) with `yieldToEventLoop()`, plus a 60s device-fetch timeout so a hung read can't leave Refresh spinning. Verified live: list icons render, no phantom spin.
- [x] **H8 (P0)** App **randomly auto-navigates to Today** with no input (full reload resetting the route). — DONE (`eeed308d`): `routePersistence` records the active route to sessionStorage and `RootRedirect` restores it after a background reload instead of snapping to Today. Verified live: the app held `#/library` through a full dev-server restart.
- [x] **H9 (P1 feature)** **Claude Code handover** — SHIPPED (2bb00e7a + 3 hardening rounds → 743b9810): a real handover BUNDLE (HANDOVER.md + transcript + summary + action-items + decisions + manifest) written into the target repo, plus in-app agentic run via the brains seam (opaque bundle ids, canonical+revalidated paths, home-dir guard, atomic allocation, real cwd, threat-modeled residual documented). Fallbacks (copy/terminal) kept. Dialog on Actionables + Today.
- [x] **H10 (P1 feature)** **Pluggable AI "brains"** — **SHIPPED + LIVE-VERIFIED** (Settings → AI Brains screenshot on the running app). Delivered: `AIBrain` abstraction + `BrainRouter` (capability-guarded routing, Gemini-first/Ollama-fallback preserved) · encrypted per-brain credential vault (closes the plaintext-key gap; atomic + self-healing sync) · **5 registered brains** — Gemini (API), Ollama, **Claude Code**, **Codex**, **Gemini CLI** (headless CLI adapters: win32 PATH/PATHEXT + cmd.exe shim exec, stdin prompts, output byte-caps, confirmed process-tree teardown, honest auth probes) · Settings panel with enable toggles, Default radio, live auth badges. Hardened through 4 Codex adversarial rounds (`5d805f00`→`c12cb8c3`→`2f8a736d`/`ea021bc5`/`35131024`→`7bb544fb`→`ed20544e`→`17c99eef`). Remaining: route more call sites through non-default brains as they're enabled (follow-on), H9 handover builds on this seam.

### H11–H14 — Titlebar #138 (2026-07-11 owner feedback) — DONE (`70d9715a`; solid `#0f1626` bar, h-14/56px + overlay synced, grouped cluster, full-width divider). ⚠️ needs app restart for the native-gutter height/color to show.
- [x] **H11 (P1)** **Gradient clashes with the flat native window buttons.** Remove the titlebar gradient → solid dark bar, AND set Electron `titleBarOverlay.color` to that same solid so the native-controls gutter blends. No seam. (Or, rejected alt: give the window buttons a matching gradient — we chose SOLID.)
- [x] **H12 (P1)** **Bar too short.** `h-10` (40px) → taller (~`h-14`/56px) to fit the two-line "Meeting / Intelligence" logo + buttons like the mockup. Must bump `titleBarOverlay.height` in `electron/main/index.ts` to match (needs app restart to take effect).
- [x] **H13 (P1)** **Right-cluster icons (bell/activity/cog/device/user) too condensed / uniformly spaced / badly placed.** Give proper rhythm: group the three icon-buttons, larger gap before the device pill, then the user menu; align on one baseline; both themes.
- [x] **H14 (P1)** **No line under the window controls.** The bottom divider stops before the native-controls gutter (`pr-[138px]` in `Layout.tsx`) → extend it FULL width so one continuous line separates the whole bar (incl. under — ▢ ✕) from the app.

### H15–H16 — Chrome placement (2026-07-11 owner, REPEATED complaints) — DONE + TRIPWIRED
- [x] **H15** **Activity Log must NOT be in the sidebar** (it's already the titlebar ⚡ button). — DONE (`a5132b5a`): removed the sidebar entry point; **deleted** `ActivityLogPanel.tsx` + test. TRIPWIRE: if a sidebar Activity Log ever reappears, remove it — the titlebar is the single owner.
- [x] **H16 (= D10)** **Collapse button in the WRONG place** (was sidebar top-right corner). Owner: "IT GOES IN THE MIDDLE OF THE SIDEBAR, VERTICALLY, MID-RIGHT." — DONE (`529bcdae`): moved OFF the titlebar onto the sidebar's right border, vertically centred at mid-height. TRIPWIRE: it must stay mid-right on the sidebar edge, never the top corner / titlebar.

### H17 — Library list layout ("for the millionth time", R144–R156) — RESOLVED + DOM-VERIFIED
Owner's four complaints (images #119–121) — all confirmed fixed via live DOM measurement (2026-07-11), evidence not just a claim:
- [x] **No horizontal scroll** — sources scroller `scrollWidth-clientWidth = 0`.
- [x] **No excess left padding** — title starts 12px from row edge; the old checkbox+status-icon left gutter (~44px) is gone (checkbox removed in H4 `f1db2204`; status icons moved to the RIGHT cluster).
- [x] **Icons on the left moved right** — `SourceRow` status/meeting icons live in the right cluster, one baseline.
- [x] **Separators reach both edges** — `border-t` on the full-width (100%) row wrapper measures `rowLeft==listLeft (224)` and `rowRight==listRight (561)` → `spansFullWidth: true`.
TRIPWIRE: the sources list must never horizontally scroll and its row separators must span the full panel width (no left/right inset). Verify with a DOM measure, not eyeballing, if re-touched.

## Track A — Titlebar / chrome polish (agent-flagged gaps)
- [x] **A1 (P1)** Wire **⌘K** to focus/open the titlebar search. — DONE (`022de7a7`): ⌘K/Ctrl+K keydown focuses+selects the titlebar search (guards against stealing focus from other fields/modals).
- [x] **A2 (P1)** Notifications 🔔 real popover. — DONE (`022de7a7`): NotificationsButton with a recent-ops popover.
- [x] **A3 (P2)** Activity ⚡ unify open-state. — DONE (`022de7a7` + `a5132b5a`/H15): the titlebar ⚡ owns the single overlay; the sidebar ActivityLogPanel was removed entirely, so there is one entry point + one state.
- [x] **A4 (P2)** User menu **About** dialog + **Brand** = home affordance. — DONE (`022de7a7`): About dialog in the UserMenu; Brand has an `onHome` → navigate('/today').
- [x] **A5 (P2)** Titlebar responsive pass: verify brand/search/cluster at narrow widths + collapsed rail; both themes. — **PASS (no defects, no fix needed).** Verified live via CDP `Emulation.setDeviceMetricsOverride` at 1000/760/560px + collapsed rail (evidence `.audit-shots/A5-w560.png`, `A5-collapsed-w760.png`). Findings: (1) no button ever goes offscreen or clips under the 138px native-controls gutter; (2) search shrinks FIRST (383→169→81px) via `min-w-0`, right cluster stays `shrink-0` and fully visible; (3) device-pill label + ⌘K kbd hint hide below `md`/`sm` (icon-only) as designed; (4) collapsing the rail reduces the brand cell w-56→w-16 and the search expands full-width, all right-cluster controls intact; (5) titlebar is intentionally theme-independent (hardcoded `#0f1626` solid dark chrome, `text-slate-100`) so light + dark render identically — both themes covered. No code change required.

## Track B — Waveform / meeting-timeline follow-ups
- [x] **B1 (P1)** Marker → transcript cross-highlight — DONE (`a246ca8e` pick): marker/event-row click scrolls the matching turn centered + 1.6s pulse (reduced-motion honored), keyboard-activatable.
- [x] **B2 (P2)** Reader-people color-key dedup — DONE: labelColorKey maps every diarization label to its representing chip key; chip swatch and bars always match.
- [x] **B3 (P2)** Timeline backfill on open — DONE: existing backfill now guarded once-per-recording-per-session (no re-run churn on legitimately-empty recordings).

## Track C — Device / sync bugs (from remaining-bugs.md; verify-then-fix — USB SAFETY)
> Do NOT probe hardware. Fix with mocks/unit tests; verify against logs only.
- [ ] **C1 (P1)** **BUG-R4**: DownloadService "Skipping X: in synced_files" log spam (1300+ lines/sync) → one summary line. (Confirmed live: reconciliation logs seen.)
- [x] **C2** **BUG-R1/R2/R3/R5** — DONE via Codex + 5 hardening rounds (latch-before-list + reset-on-failure; conditional terminal scan status). **BUG-R1/R2/R3/R5**: verify whether the file-list re-scan loop + double auto-sync are already fixed by recent commits; if not, set `autoSyncTriggeredRef` before `listRecordings` + stop emitting 'ready' from the finally block.
- [x] **C3** — DONE via Codex + hardening (byte-boundary settlement, quarantine + generation-owned bounded auto-reconnect, durable cancel_reason v40, age-based prune). **BUG-R8** `downloadFile()` no timeout (`jensen.ts:1458`); **BUG-R9** cancel uses 'failed' not 'cancelled'; **BUG-R10** onNewFiles never passed; **BUG-R11** unused `step1Success`.
- [ ] **C4 (P1)** **BUG-R6/R7**: Chromium USB / Autofill stderr noise — suppress via launch redirect or accept as cosmetic (document the decision).
- [ ] **C5 (arch)** **BUG-R13 / DevicePipeline**: collapse scattered device-action policy gates into one coordinator. — **SPEC DONE** (`docs/specs/2026-07-11-device-pipeline-spec.md`, via Codex; 18 cited commits independently verified). KEY FINDING: an additive-but-inert Phase-2 `device-pipeline.ts` (+instance, IPC, hook, tests; commits 0212ca3e/177419db/4fabe72c/015012b2) ALREADY EXISTS — the spec's plan is to harden + activate it behind a flag (phases 0–6, each with rollback), not build greenfield. Next: Phase 0 characterization tests after owner reviews the spec.

## Track D — Test / infra hygiene
- [x] **D1 (P1)** — DONE (996f580f pick): QA-log constructor loop stubbed; 4 clean full runs. Original text: `src/services/__tests__/hidock-device.test.ts` vitest **worker-teardown race** ("Closing rpc while onUserConsoleLog pending") — quiet teardown logging or isolate that file's pool so the full suite is 0-error.
- [x] **D2 (partial, ongoing)** — 18 mechanical warnings cleared (125→107, 0 errors). Original text: Address the 122–125 pre-existing eslint warnings (unused caught errors, no-useless-escape) incrementally.
- [x] **D3 (P1) — RESOLVED (996f580f pick): global vi.mock in src/test/setup.ts redirects better-sqlite3 to the Node-ABI workspace copy — real SQLite, no stubs, app binding untouched. FULL SUITE 3331/3331, 0 failures. Original ACTION ITEM (infra, caused by tonight's dev-server restart).** The 19 DB-dependent vitest files (~141 failures) fail with `better-sqlite3` **NODE_MODULE_VERSION 140 vs 147** ABI mismatch: `node_modules/better-sqlite3` is built for the running Electron app (ABI 140), but Node-based vitest needs ABI 147. Proven pre-existing to the brains change (identical failure set on `77b7354b` before cherry-pick). Dual-ABI conflict — one build can't satisfy both. FIX OPTIONS: (a) mock `@hidock/database`/`better-sqlite3` in the DB tests so they're ABI-independent (best); (b) a test script that `npm rebuild better-sqlite3` for Node before the suite + electron-rebuild after; (c) run DB tests only in CI with the app not running. **Do NOT rebuild for Node while the app is being dogfooded — it breaks the running app's DB on its next restart.** Non-DB suites (brains, layout, waveform, etc.) are unaffected and green.

- [ ] **D4 (P2) — flake watch.** Chat.test.tsx failed ONCE (1-in-4) when run combined with src/components/assistant immediately after cherry-pick 215fbbda under heavy parallel-agent load; 4 subsequent runs (single + 3x combined) all green. Suspect the timing-sensitive ResizeObserver/font-size test. Repro: `npx vitest run src/pages/__tests__/Chat.test.tsx src/components/assistant` in a loop under load. If it recurs, deflake the observer test (fake timers / explicit flush), do NOT skip it.

- [ ] **D6 (P2, CI) — packaged-app / Electron-runtime DB query smoke.** Review follow-up to D3: the dual-ABI shim is now SCOPED to the `main-db` vitest project (src/test/setup-db.ts), and an UNMOCKED `native-binding` project (`better-sqlite3-binding.smoke.test.ts`) asserts the production Electron-ABI binary EXISTS, that loading it under Node fails only with the specific ERR_DLOPEN_FAILED / NODE_MODULE_VERSION pair mismatch (anything else = missing/corrupt binary = red), and that both workspace copies stay version-pin equal (12.11.1). What Node-based vitest CANNOT do by definition is execute a query through the Electron-ABI binary — that requires the Electron runtime. TODO: add a CI job that launches the packaged app (or `electron -e`) and runs one real `SELECT 1` through the app's own better-sqlite3 to close the last gap.

- [x] **D5 (P1, LESSON 2026-07-11) — agents MUST stop their preview/harness vite servers before reporting (Definition of Done).** Three leaked vite servers were found spinning at ~1 full core EACH (~74% total system CPU with tsserver): agents `agent-ac002862f1803c518` and `agent-a5dfe5070a79b4fda` left `vite --config vite.preview.config.mts` running from their worktrees, plus one `vite --config .wf-harness/vite.config.ts` in the main repo. MECHANISM: the agents (or later cleanup) deleted the config files/`.wf-harness/` dir while the server kept running — vite watches its own config for restart, and with the config gone it enters a restart/error busy-loop, burning 100% CPU with NO listening socket (invisible to port checks). PREVENTION: (1) every agent brief that starts a vite preview/harness server must include stopping it (devproc or KillShell) as a pre-report step; (2) never delete a worktree or harness config while its server may still be running — stop the server FIRST; (3) periodic sweep: any `vite` process whose `--config` file no longer exists is an orphan — stop it via `devproc.mjs register <pid> --port <p> --label orphan-cleanup` + `stop <pid>`. Cleaned 2026-07-11 (PIDs 67836/93116/59872); system CPU dropped ~74% → 5–15%.

## Track E — Cross-surface audit loop (expands this list)
Dogfood each un-hardened surface, file findings back here, fix, re-walk:
- [ ] **E1** Calendar · **E2** Context Graph · **E3** Projects · **E4** Explore · **E5** Settings · **E6** Actionables · **E7** Sync/Device · **E8** Assistant/RAG
- Per surface: clickability / editability / discoverability + liveness + hierarchy; log new items as F-series.

## Track F — Discovered during audit (append as found)
- [x] **F1 (P1, owner-reported 2026-07-11, img #142)** **Projects → Identity suggestions: every KEEPS/ALIAS row shows "Couldn't check transcripts."** — DONE (`af08ffd7`). ROOT CAUSE: renderer queue-starvation — the hook fired one 5s-timeout mention lookup per pending suggestion of EVERY kind in one Promise.all against a serial sql.js queue (~178ms each, ~125s total); low-confidence project suggestions sort to the tail → all timed out → cached error. FIX: kind-scoped fetches + concurrency cap 6. LIVE-VERIFIED: 69× error → 0 on /projects (5 real-evidence + 64 neutral). On the Projects merge/identity-suggestions panel ("N names may be X: … (67%)"), each suggestion card (both the KEEPS project and each ALIAS candidate) renders an amber "Couldn't check transcripts" warning — systematic, not occasional. Likely the transcript-evidence check for PROJECT-entity merge suggestions is erroring or querying the wrong table/id (works for people/speaker identity, fails for projects). Investigate the project-suggestion transcript lookup (identity-discovery / suggestions handler) — is it calling a transcripts query that throws or returns empty for project entities? Fix so it shows real transcript evidence (or hides the line when genuinely none), not a blanket error. Verify live on the Projects page.

### F2–F5 — Assistant × Brains × RAG (owner questions 2026-07-11, img #142) — honest answer was "not thoroughly checked"
- [x] **F2 (P1)** **Assistant placement matrix dogfood + overlay layout bug.** — DONE (`ec0296c0`). ROOT CAUSE: GlobalAssistant renders the full desktop Chat page inside the 22rem overlay; Chat had a fixed 256px history sidebar → chat column crushed to ~96px. FIX: Chat is now @container-responsive (sidebar → History drawer below @lg, compact header, truncate+hide caption). Matrix walked: floating L/R were broken (now fixed), embedded L/R were fine. LIVE-VERIFIED post-HMR on /today. Owner's screenshot shows the FLOATING overlay rendering the Chat squeezed into a broken two-column layout (page content bleeding behind, send column crushed to ~80px with wrapped caption text). Walk ALL four combos — floating/embedded × left/right — fix the overlay layout, verify pin-to-embed and collapse behaviors, both themes, live via CDP.
- [ ] **F3 (P1)** **Assistant E2E with EACH brain.** Prove a chat actually routes through the selected default brain: enable Claude Code / Codex / Gemini CLI one at a time, send a real prompt from the Assistant, verify the reply came from that brain (and that failures degrade honestly). Revert default to Gemini afterward. Nobody has verified the router→agentic-brain path from the UI.
- [ ] **F4 (P1)** **Brains awareness of RAG + Context Graph.** Today rag.ts injects only 1-hop neighborhoodFacts when the message literally names an entity (rag.ts:314-318) — verify that context actually reaches whichever brain is selected, then DEEPEN: graph facts for implicit references, meeting/project scoping, and document the context budget per brain.
- [x] **F5 (P2 feature)** — DONE (ff03abc9 pick; hardened 93ad0ec3 fail-closed columns/durable state/diversity rerank; final fc7eadfe moves backfill eligibility INTO SQL — json_extract predicates so terminal/cooling rows never occupy the scan window, 520-terminal-rows starvation regression test): images are first-class RAG sources — existing vision path reused, chunks tagged sourceType+captureId, [Screenshot: …] context labels + source chips w/ captureId, bounded boot backfill; no schema bump. Gap: no capture-detail route yet (captureId threaded for future deep-link). Original: **PixelRAG** — image/screenshot retrieval into assistant context. Clipboard screenshots + image captures exist as artifacts but are NOT in the vector store and can't be retrieved by the RAG (0 image/vision references in rag.ts / vector-store.ts). Design: OCR/vision-index image captures (Gemini API brain has vision), embed the extracted text, retrieve alongside transcript chunks, and let the assistant cite the source image.

### F6–F11 — Cross-surface dogfood walk (Track E, 2026-07-11, agent-a076 via CDP) — FILED, NOT FIXED
Walked E1–E8 live. Healthy/no-finding surfaces: **E5 Settings** (Assistant placement, Calendar ICS w/ live "Last synced", Connectors, AI Brains "Loading…" was transient), **E6 Actionables** (3176 pending; filters/sort/group/generate/dismiss all work — initial 1-card flash was a CDP screenshot racing the async load, not a defect), **E7 Sync/Device** (H1E connected, storage/recordings/device-settings/Format-Storage(red, labelled)/per-file status all honest; state updated live — Calendar's earlier "(device not connected)" correctly cleared once auto-connect landed). **E8 Assistant** rich + honest (Gemini · 109778 chunks, real transcript-grounded answer, Context/Chunks/history). New findings below:

- [x] **F6 (P2, E1 Calendar)** — DONE (52797d76): cascade layout via `assignOverlapLanes()` (greedy interval colouring) — overlapping blocks stay full-width per the owner's July-8 "no Outlook shrink" ruling, each later block indents 10px/lane with higher z-order so every title row reads; hover/focus brings any block to front; non-overlapping days render unchanged; 8 unit tests. NOTE: main had grown an Outlook-style column split that violated the ruling — replaced. Original: **Overlapping/concurrent calendar events collide instead of laying out side-by-side.** In the Work/week view, events that overlap in time render stacked on the SAME column so their titles/times overlap and become unreadable (evidence `.audit-shots/E1-calendar.png` — TUE 7 ~2 PM "Engineering + HR meeting" text collides with "Devops | Revisión alcance vs…"; MON 6 stacked "Daily Onboarding" pair; DOM check confirmed ≥3 colliding box pairs with <30px left-delta and vertical overlap). No column-splitting for concurrent events. Data is present but obscured. Fix: lay concurrent events out in side-by-side sub-columns (standard week-view collision layout).
- [x] **F7 (P2 a11y, E1 Calendar)** — DONE (52797d76): aria-labels with subject + spoken time range on every event surface (week/day recording blocks, meeting overlays, month buttons), focus-visible rings + focus z-top, list cards get role="button"/tabIndex/Enter/Space/aria-pressed, checkbox buttons labelled, dishonest cursor-pointer removed. Original: **Event cards are mouse-only.** Cards have `cursor:pointer` and navigate to `#/meeting/:id` on click (functional), but carry no `role` and no `tabindex` → not keyboard-focusable and invisible to screen readers. Fix: add `role="button"` + `tabIndex={0}` + Enter/Space handlers (or render as a real button/link).
- [x] **F8 (P2, E2 Context Graph)** — DONE (round 1 + round 2 bfcbb834): ordinal time scale spreads nodes across the full band width; round 2 made the axis HONEST — sparse labelled date ticks (oldest + newest + every Nth), subtitle says "ordered by time" (sequence, not scaled), `buildTimeScale()` bins adjacent ranks when density exceeds the 28-unit node-diameter floor (1000-timestamp fixtures). Original: **"Your context" lens crams all nodes into the far-right time column; DECISIONS & WORK strata bands render visually empty.** The reasoning-strata legend claims "Decisions · 20 of 1007 · Work · 25 of 1816 · People · 20 · Meetings · 20" in the 30d slice, but the canvas (canvas-rendered, 1914×1224) shows nodes only clustered at the bottom-right edge (near the "Sebas" node) with the labelled DECISIONS and WORK bands empty and ~60% of the canvas blank (evidence `.audit-shots/E2-context-graph.png`). Weak spatial hierarchy — the strata the screen advertises don't visibly contain their nodes. Investigate the strata Y-placement + time-X packing so nodes land in their labelled band and use the horizontal space.
- [x] **F9 (P2, E3 Projects)** — DONE (round 1 + round 2 bfcbb834): provenance chips link discovered projects to their source meetings; round 2 made dismissal DURABLE — new `project_discovery_rejections` tombstone table (schema v41), `projects:dismissDiscovered` IPC, org-reconciler auto-create checks the tombstone so a dismissed name is never re-created by re-analysis, manual create clears it; real-DB regression proves the full cycle. Manual empty projects get a neutral card (not "Discovered automatically"). Original: **Auto-discovered project detail is a dead-end with no provenance.** Opening a discovered project (e.g. "Odessa Fire Incident Investigation") shows only "KNOWLEDGE 0 Items / PEOPLE 0 Involved" and no link back to the meeting/transcript it was discovered FROM (evidence `.audit-shots/E3b-project-detail.png`). Two problems: (a) the hub can't surface its own source meeting; (b) several discovered projects look spurious/topic-shaped ("Odessa Fire Incident Investigation", "Kansas COVID-19 Public Response") and carry 0 items/0 people — discovery may be over-generating empty shells. Fix: link discovered projects to their source captures and surface provenance; consider suppressing 0-evidence discoveries. (F1's "Couldn't check transcripts" is confirmed FIXED — now reads "No verbatim mentions — extracted from meeting analysis".)
- [x] **F10 (P1 dishonest data, E4 Explore)** — DONE (recurring-topics.ts service + Explore wiring): chips derive from real topic frequency over recent meetings; honest empty state when no data. Original: **"RECURRING TOPICS" is a hardcoded placeholder, not real data.** `apps/electron/src/pages/Explore.tsx:246` renders a literal array `['Amazon Connect','API Design','Migration','Q1 Planning','Security']` under the caption "Topics frequently mentioned in your recent meetings." These generic English topics do NOT reflect the user's ~1900 Spanish DFX5 meetings (real topics: Antamina, S2D, Connect, Onboarding, Belcorp…). The caption asserts something false. NOTE: the Explore SEARCH itself is real and fast (typed "Antamina" → 21 results in 2ms across Knowledge/People/Projects). Fix: derive the chips from actual topic frequency (or drop the section) — do not present static demo strings as "your recent meetings".
- [x] **F11 (P2, E8 Assistant)** — DONE (App.tsx GlobalAssistant): floating bubble suppressed on /assistant (and /library, which owns its own), with trailing-slash normalization. Original: **Floating "AI Assistant" bubble is redundant on the dedicated /assistant page.** The GlobalAssistant floating button renders on every route including the full Assistant page, so on `#/assistant` a user can open a second (overlay) assistant on top of the full one (evidence `.audit-shots/E8-assistant.png`). Fix: hide the floating bubble when the active route is the Assistant page.

## Track I — Modular Features, Presets & Performance (owner feature, 2026-07-11)
Owner: not everyone wants (or has hardware for) full Meeting Intelligence. Every FUNCTION must be toggleable — disabling kills its background tasks AND its UI surfaces, with dependency cascade (disable Context Graph → dependent People/Projects/Calendar affordances follow). Connectors (GitHub — currently not even configurable! — Slack, Outlook/M365, ICS) all get config+toggle. Presets: "HiDock library management only" / "HiDock + Transcription" / "Full Context Awareness". Plus: per-feature PERFORMANCE instrumentation (load, running, surface-switch, source-processing) → optimization path + hardware guide; and (privacy-gated, opt-in) usage/perf telemetry to inform local-vs-web service offering.
- [x] **I1 (P1)** — DONE (docs/specs/2026-07-11-modular-features-spec.md): 10-feature inventory w/ tasks/surfaces/deps, GitHub-gap evidence (process.cwd() repo paths, no contract/toggle), registry + 4 enforcement gates + soft cascade, presets w/ runtime-vs-restart table, perf-meter at 6 choke points, off-by-default telemetry. Original: Design spec: feature registry (id, background tasks, surfaces, IPC, dependency graph), toggle semantics, presets, connector config/toggle unification (incl. the GitHub gap), perf instrumentation points, telemetry consent model.
- [ ] **I2** Feature registry + flags + dependency cascade (config-backed, UI + background enforcement).
- [ ] **I3** Settings: Features panel + presets; connector config/toggles unified.
- [ ] **I4** Perf instrumentation per feature + local report surface (the optimization/hardware guide data).
- [ ] **I5** Opt-in usage/perf telemetry (explicit consent, anonymized, off by default).

## Track G — Security / release (owner-gated)
- [ ] **G1** Promote the Dependabot security fix (0-vuln) from this branch to `main` — **owner's call** (QA-first policy; do not push to main unprompted).

---

### Working order (tonight)
1. Audit loop (E) in parallel to expand the backlog → append to F.
2. Safe, high-value, low-risk first: A (titlebar polish), C1/C4 (log noise), D1 (flaky test).
3. Then B (timeline follow-ups), C2/C3 (device bugs, verify-first).
4. Architectural (C5 DevicePipeline) scoped last — spec before building.
5. G1 held for the owner.

- [ ] **F12 (P2, from F9 investigation)** Spurious project auto-creation: org-reconciler.ts:534-552 creates a REAL projects row for ANY non-empty extracted name below SUGGEST_THRESHOLD resolution — no confidence floor, no seen-in-≥2-meetings gate → one-off phrases become zero-item dead-end projects. Add a gate (confidence floor / recurrence / defer-to-suggestion). F9 dismiss/merge UX prunes them meanwhile.
- [ ] **D7 (P2, flake)** transcription.test.ts (vibevoice) — own database mock omits runInTransaction and async processQueue bleeds across tests; flakes under altered worker scheduling, deterministic in isolation. Harden the mock + teardown.
- [ ] **F13 (P1 perf/freeze, owner 2026-07-11)** **Calendar sync must be INCREMENTAL, not a full resync.** The periodic calendar sync (every few minutes) re-syncs all 1800+ events each run and freezes the app for seconds-to-minutes. If events are already stored and unchanged, do NOT resync them. Find the proper mechanism: delta tokens / incremental sync where the source supports it (M365 Graph calendarView delta), else changed-since filtering + content-hash comparison so unchanged events are skipped without touching the DB/UI, chunked + yielded processing off the critical path so the renderer never blocks, and a visible "last synced / N changed" status instead of a full-churn pass. Measure before/after (ties into Track I perf-meter I4).
- [ ] **F15 (P1 freeze, owner 2026-07-11, evidence img #145)** **App fully "Not Responding" for 30+ seconds during boot** — window frozen at ~3.6 GB memory while `[BootScheduler] running "meeting-wiki-backfill"` was active, with the periodic calendar sync running in parallel (it then failed twice: `TypeError: fetch failed` / `read ECONNRESET` inside syncCalendar, while a Jensen getRecordingFile download was also streaming). Suspects, in order: (a) meeting-wiki-backfill doing heavy synchronous DB/file work on the main process (blocks ALL IPC → renderer paints nothing → "Not Responding"); (b) the full calendar sync (F13) churning 1800+ events concurrently on the main thread; (c) sql.js persistence write amplification under both at once. Investigate with per-boot-task timing (BootScheduler already logs durations — capture meeting-wiki-backfill's), chunk/yield the backfill, and never run calendar sync concurrently with boot tasks. Related: F13 (incremental sync), Track I perf-meter (I4) would have measured this. Also note: calendar sync must survive transient ECONNRESET with backoff instead of failing the whole pass.
- [ ] **F14 (P2 feature, owner 2026-07-11)** **Activity Log: add "Copy to clipboard" and "Save to disk".** The titlebar Activity Log needs (a) copy-all (and ideally copy-selection) to clipboard, (b) save/export to a file on disk (default filename with timestamp; owner picks location via save dialog). Respect current filters if any — what you see is what you copy/save.
- [ ] **D8 (P2, dev-tooling, Codex accidental-review find 2026-07-11)** (a) `npm run check:native -- --help` performs a DESTRUCTIVE native-addon rebuild (check-native.mjs:64-68 never parses args before the rebuild path) — parse --help/-h first, print usage, exit 0, reject unknown args. (b) app-cycle.mjs:229-234 + cdp.mjs:139-140 generate --help by slicing fixed source-line ranges, leaking implementation and breaking on any edit — replace with explicit usage strings.

### F16–F19 — Meeting Value Index + honest deletion (owner 2026-07-13, "Almuerzo" incident)
Context: a 32.5-min kitchen conversation (`2026Jul13-130950-Rec51.hda`) was captured because the device records whenever the mic/audio session OPENS (owner-confirmed: he joined a no-show meeting after his daily and walked away; the open "ready to join" session kept the device recording — not scheduled, not calendar-fired; the electron app has no record-start or CMD-20 path at all). The capture was then transcribed, summarized, embedded (20 RAG chunks), and graph-ingested (cooking steps became `action_item`/`next_step` nodes), sitting at `quality_rating='unrated'`. Low-value captures are INEVITABLE with a mic-follows-you device → software must value-gate them (F16) and deletion must be honest + complete (F17/F18).

- [x] **F16 (P1 feature, owner 2026-07-13)** — **DONE 2026-07-14** (branch `claude/f16-f17-value-index-deletion`, 37 commits `907732b9..160a6dcf`, phased-dev + double review [Opus + /codex:review] every increment + Codex adversarial plan review, live-QA PASSED 9/9 on an isolated instance, suite 3510→3766 green): content-based value classification rides the existing analysis call (kill-switch `transcription.valueClassificationEnabled`, confidence floor 0.6, escape-proof untrusted-data delimiting, never-downgrade w/ `quality_source`), migrations v42+v43, gates live on RAG (query-time union) + graph ingest (pre-filter + transactional point-read) + actionables (pre+post-await checks), Library badge/legend/filter/manual-rating, coalesced suggestion toasts, Settings-triggered resumable backfill (reserve→raw→finalize atomic, crash-boundary tested, attempts-capped, privacy-rechecked per item, never a boot task). Deviations by design: high/normal never auto-write a rating; backfill is Settings-only and independent of the live kill-switch (hint shown); retro-purge of already-ingested graph/actionables deferred to F18/F17. **Meeting Value Index — content-based value classification + downstream de-prioritization.** Nothing content-aware exists today: (a) `classifyLowValueCaptures()` (database.ts:3875) only marks <20 s + <20-word + no-meeting captures (deliberately narrow); (b) `QualityAssessmentService.inferQualityFromData()` (quality-assessment.ts:129) scores METADATA only — it would rate the Almuerzo capture HIGH (has transcript + meeting link + good duration + valid size); (c) `recordings.personal` is manual-only. Rec51's capture sits at `quality_rating='unrated'`. BUILD: emit a value classification from the existing post-transcription analysis (one extra field in the same LLM call): `value: high|normal|low|none` + reason tags (personal/family · greeting-only/no-show · background/ambient · no-substance) + confidence, mapped onto the existing `knowledge_captures.quality_rating` taxonomy (`low-value`/`garbage`; Library filter/sort already support it). GATES (the actual point): below threshold → skip graph ingest (knowledge-graph-service.ts:202 already filters `personal` — extend to rating), skip actionable extraction, exclude/downweight in RAG retrieval (vector-store already filters excluded ids — extend), Library badge + suggest "Mark personal?" toast. Never auto-delete; never downgrade a user-set rating (classifyLowValueCaptures already honors this). Backfill: rate-limited, resumable batch pass over the ~1,900 existing captures.
- [ ] **F17 (P1 UX/privacy, owner 2026-07-13)** **Deletion is mislabeled and incomplete — no true "delete everything".** Verified: (a) row-menu **"Delete everywhere"** (SourceRow.tsx:281, synced rows) actually runs the SOFT delete (`deleteCascade(id,false)` → Library.tsx:831): sets `deleted_at`, hides + stops AI, keeps audio + every derived row, restorable — it deletes NOWHERE. (b) **"Delete permanently…"** = hard purge (recording-deletion-service.ts / database.ts:3227): removes transcripts+embeddings+vector chunks+captures&children+actionables+speaker data+synced_files + audio file + wiki export + artifact blobs — but NOT the device copy (intentional, USB safety), NOT the knowledge graph (F18), NOT `graph_ingested_transcripts`, and `deletion_journal` keeps a full metadata snapshot. (c) A synced row has NO menu path to delete its device copy (only device-only rows get "Delete from device"; otherwise Device page only). FIX: 1) honest labels — "Move to Trash (hide + stop AI)" / "Delete from device" / "Delete permanently…" — scope stated in the menu, not only the confirm; 2) permanent-delete dialog gains an "Also delete from device" checkbox when connected (single Jensen DELETE_FILE, no probing) so ONE flow removes everything; 3) hard purge also cleans the graph (F18) + `graph_ingested_transcripts`; 4) document (or purge) the `deletion_journal` snapshot.
- [x] **F18 (P2 data-quality, found 2026-07-13)** — **DONE 2026-07-14** (branch `claude/f16-f17-value-index-deletion`, 7 commits `b6254f90..3585358a`, phased-dev + double review + Codex adversarial plan review [3 HIGH + 2 MEDIUM accepted pre-code], live QA PASSED incl. real-file end-to-end): id-keyed meeting nodes (`meeting:<id>`, per calendar occurrence; legacy label-keyed history accepted as-is), `graph_edge_sources` provenance side table (package GRAPH_SCHEMA, assertion_count; migration v44 unconsumed), transactional `removeRecordingProvenanceCore` (F17 embeds it INSIDE the purge txn — all-or-nothing per AR2-2) + `removeRecordingFromGraph` dry-run wrapper (counts = estimates), provenance transfer at all 4 edge-repoint sites, clean edge-deletion cascade everywhere, purge-vs-ingest race closed via existence guard (interleaving-table verified), never-over-delete rules (person always protected; unattributed edges never deleted — full-weight attribution required). Deviations: engines>=20 (better-sqlite3 12.x); `unattributedResidueKept` counter; F17/T6 wiring deferred to Phase 3. **Graph meeting nodes are keyed by TITLE → recurring meetings collapse into one node; no per-recording provenance → precise graph cleanup impossible.** `ingestExtraction()` (packages/knowledge-graph/src/ingest.ts:54) upserts meeting nodes with no `key`, so `upsertNode` (graph-store.ts:118-141) dedups by `normalizeLabel(label)`: every "Almuerzo"/"Daily Onboarding"/"CX - Weekly" occurrence folds into ONE node, `props.meetingId` last-writer-wins. Verified live: the single 'Almuerzo' node carries 54 edges mixing the Jul-13 cooking capture with a prior work lunch (candidate interviews, Lenovo laptops, Kantata→Jira) — cross-day soup, and F17's purge cannot attribute edges to a recording. FIX: key meeting nodes by meeting/recording id (mirror person nodes' `contact:<id>` pattern), record per-edge provenance (source recording/transcript id in edge props or a side table), migrate label-keyed nodes (retroactive split impossible — accept merged history or re-ingest), then give hard-delete a precise path: drop that meeting node's edges + GC orphaned topic/decision/action_item/next_step nodes.
- [ ] **F19 (P2 security, found 2026-07-13)** **Plaintext secrets remain in `config.json`** (`transcription.geminiApiKey`, `localAsrHfToken`) while `calendar.icsUrl` is already `__enc__`-encrypted and brain credentials moved to the encrypted vault (H10). Migrate the remaining keys to the same safeStorage pattern with transparent read-back + one-time upgrade-on-write.
